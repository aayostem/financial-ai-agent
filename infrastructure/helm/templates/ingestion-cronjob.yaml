{{- if .Values.ingestion.enabled }}
---
# Ingestion — CronJob
apiVersion: batch/v1
kind: CronJob
metadata:
  name: {{ include "financial-ai-agent.fullname" . }}-ingestion
  namespace: {{ .Values.namespace }}
  labels:
    {{- include "financial-ai-agent.labels" . | nindent 4 }}
    app.kubernetes.io/component: ingestion
spec:
  schedule: {{ .Values.ingestion.schedule | quote }}
  concurrencyPolicy: {{ .Values.ingestion.concurrencyPolicy }}
  successfulJobsHistoryLimit: {{ .Values.ingestion.successfulJobsHistoryLimit }}
  failedJobsHistoryLimit: {{ .Values.ingestion.failedJobsHistoryLimit }}
  jobTemplate:
    spec:
      backoffLimit: 2
      activeDeadlineSeconds: 10800
      template:
        metadata:
          labels:
            {{- include "financial-ai-agent.selectorLabels" . | nindent 12 }}
            app.kubernetes.io/component: ingestion
          annotations:
            {{- include "financial-ai-agent.podAnnotations" (dict "Values" .Values "componentAnnotations" (default dict .Values.ingestion.podAnnotations)) | nindent 12 }}
        spec:
          restartPolicy: {{ .Values.ingestion.restartPolicy }}
          serviceAccountName: {{ include "financial-ai-agent.fullname" . }}-ingestion
          securityContext:
            runAsNonRoot: true
            runAsUser: 1000
            fsGroup: 1000
          {{- with .Values.global.nodeSelector }}
          nodeSelector:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          {{- with .Values.global.tolerations }}
          tolerations:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          containers:
            - name: ingestion
              image: {{ include "financial-ai-agent.image" (dict "global" .Values.global "image" .Values.ingestion.image) }}
              imagePullPolicy: {{ .Values.global.image.pullPolicy }}
              env:
                {{- range $k, $v := .Values.ingestion.env }}
                - name: {{ $k }}
                  value: {{ $v | quote }}
                {{- end }}
              {{- with .Values.ingestion.envFrom }}
              envFrom:
                {{- toYaml . | nindent 16 }}
              {{- end }}
              resources:
                {{- toYaml .Values.ingestion.resources | nindent 16 }}
              securityContext:
                allowPrivilegeEscalation: false
                readOnlyRootFilesystem: true
                capabilities:
                  drop: ["ALL"]
              volumeMounts:
                - name: tmp
                  mountPath: /tmp
          volumes:
            - name: tmp
              emptyDir: {}
---
# Ingestion — ServiceAccount
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "financial-ai-agent.fullname" . }}-ingestion
  namespace: {{ .Values.namespace }}
  labels:
    {{- include "financial-ai-agent.labels" . | nindent 4 }}
  annotations:
    eks.amazonaws.com/role-arn: arn:aws:iam::ACCOUNT_ID:role/financial-ai-ingestion
{{- end }}